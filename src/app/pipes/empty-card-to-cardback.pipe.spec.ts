import { EmptyCardToCardbackPipe } from './empty-card-to-cardback.pipe';

describe('EmptyCardToCardbackPipe', () => {
  it('create an instance', () => {
    const pipe = new EmptyCardToCardbackPipe();
    expect(pipe).toBeTruthy();
  });
});
