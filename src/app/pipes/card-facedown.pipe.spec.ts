import { CardFacedownPipe } from './card-facedown.pipe';

describe('CardFacedownPipe', () => {
  it('create an instance', () => {
    const pipe = new CardFacedownPipe();
    expect(pipe).toBeTruthy();
  });
});
